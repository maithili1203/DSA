#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int length(char[])
void main()
{
    char str[10][10];
    int i,j;
    int a[10];
    for(i=0;i<10;i++)
    {
        gets(str[i]);
    }
    printf(" The longest line is: ");
    puts(str[i]);
    return;

}
void compare(char str[])
{


j=a[0];

    for(i=0;i<10;i++)
    {
        a[i]=length(str[i]);
    }

    for(i=1;i<10;i++)
    {
        if(a[i]>j)
        {
            j=a[i];

        }
    }
    return i;
}

int length(char a[100])
{
    int i;
    for(i=0;i<10;i++);
    {
     while(str[i]!='\0')
     }
     )
    return i;
}
